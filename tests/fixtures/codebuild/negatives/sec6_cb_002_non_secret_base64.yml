version: 0.2

env:
  variables:
    BUILD_ENV: "production"

phases:
  build:
    commands:
      - npm ci
      - echo $BUILD_ENV | base64 > env.b64
      - npm test

artifacts:
  files:
    - "dist/**/*"
