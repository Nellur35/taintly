version: 0.2

phases:
  install:
    commands:
      - npm ci --ignore-scripts
  build:
    commands:
      - npm run build

cache:
  # key: build-$CODEBUILD_WEBHOOK_HEAD_REF
  key: build-stable
  paths:
    - node_modules/**/*

artifacts:
  files:
    - 'dist/**/*'
