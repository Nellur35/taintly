version: 0.2

phases:
  install:
    commands:
      - npm ci --ignore-scripts
  build:
    commands:
      - npm run build

cache:
  key: build-stable
  fallback-keys:
    - 'build-${CODEBUILD_SOURCE_VERSION}'
  paths:
    - node_modules/**/*

artifacts:
  files:
    - 'dist/**/*'
