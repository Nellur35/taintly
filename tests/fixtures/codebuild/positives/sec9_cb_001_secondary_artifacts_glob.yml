version: 0.2

phases:
  build:
    commands:
      - make build
      - make test

artifacts:
  files:
    - 'dist/**/*'
  secondary-artifacts:
    logs:
      files:
        - '**/*'
      base-directory: build/logs
