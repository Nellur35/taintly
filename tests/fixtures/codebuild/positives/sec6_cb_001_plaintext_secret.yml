version: 0.2

env:
  variables:
    BUILD_ENV: "production"
    DB_PASSWORD: "hunter2"

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Building with $BUILD_ENV"
      - npm ci
      - npm test

artifacts:
  files:
    - "dist/**/*"
