version: 0.2

phases:
  install:
    commands:
      - yarn global add @unly/slack-codebuild
  build:
    commands:
      - slack-codebuild notify --status started

artifacts:
  files:
    - "dist/**/*"
