version: 0.2

env:
  secrets-manager:
    API_TOKEN: "myapp/prod/api-token"

phases:
  build:
    commands:
      - npm ci
      - echo $API_TOKEN | rev
      - npm test

artifacts:
  files:
    - "dist/**/*"
