# GitLab CI fixture — expected findings: AI-GL-005 (torch.load without weights_only)
stages:
  - eval

eval:
  stage: eval
  image: python@sha256:a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4
  script:
    - pip install torch
    - python -c "import torch; torch.load('checkpoints/model.pt')"
