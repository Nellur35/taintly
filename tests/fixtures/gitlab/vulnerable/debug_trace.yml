# debug_trace.yml — Expected findings: SEC7-GL-001 (CI_DEBUG_TRACE)
variables:
  CI_DEBUG_TRACE: "true"
  APP_VERSION: "1.0.0"

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - make build

deploy:
  stage: deploy
  script:
    - ./deploy.sh "$APP_VERSION"
  only:
    - main
