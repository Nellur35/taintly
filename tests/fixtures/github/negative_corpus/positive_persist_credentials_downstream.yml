name: persist credentials downstream anchor

on:
  pull_request_target:

permissions:
  contents: write

jobs:
  push_back:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git push origin HEAD:auto-fix
