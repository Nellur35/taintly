# Same flow-style shape as ``flow_style_step_uses.yml`` but with a
# SHA-pinned action reference — must NOT fire SEC3-GH-001.
# Symmetric assertion confirming the structural form's predicate
# applies correctly inside flow-mapping steps.
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: [{uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd}, {run: echo build}]
