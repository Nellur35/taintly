__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
# pytest-cov artefacts — local run output, never committed.
.coverage
.coverage.*
htmlcov/
coverage.xml
# Subagent worktree infrastructure — created by Claude Code when
# isolation="worktree" is used. Pure runtime state, never versioned.
.claude/

# Generated validation artifacts, corpus outputs, reviewer notes,
# and third-party-identifying scan evidence are not product assets.
docs/field-validation/
field-validation/
corpus-output/
corpus-outputs/
review-notes/
*.field-validation.json
*.field-validation.md
